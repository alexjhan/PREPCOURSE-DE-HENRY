// Función: suma tres
    function sumaTres(x) {
        return (x + 3);
    }

// Función: Cuidado con el console log
    function cuidadoConElConsoleLog(params) {
        console.log(nombre);
        return (nombre);
    }
